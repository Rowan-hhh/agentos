unit-test:
  image: python:3.12-slim
  before_script:
    - python -m pip install --upgrade pip
    - pip install --no-cache-dir -r requirements.txt pytest
  script:
    - PYTHONPATH=. python -m pytest tests/ -v -k "not integration"
